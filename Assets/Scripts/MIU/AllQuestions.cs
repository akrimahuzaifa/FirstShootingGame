using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AllQuestions : MonoBehaviour
{
/*    int[] a = { 8, 7, 3, 4, 5 };

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log(ArrayChecker(a));
    }

    //{1, 2, 3, 4, 5}
    int ArrayChecker(int[] integerToCheck)
    {

        if (integerToCheck.Length % 2 == 0 || integerToCheck.Length == 0)
        {
            return 0;
        }
        else
        {
            int middleIndex = integerToCheck.Length / 2;
            //Debug.Log(middleIndex);
            for (int i = 0; i <= integerToCheck.Length; i++)
            {
                if (integerToCheck[i] < integerToCheck[middleIndex])
                {
                    return 0;
                }
            }
            return 1;
        }
    }*/



}

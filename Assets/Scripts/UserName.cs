using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UserName : MonoBehaviour
{
    Text userNameToShow;

    // Start is called before the first frame update
    void Start()
    {

        //Debug.Log(StartGame.userName);
    }
}
